// CartItem component
export function CartItem({ children }) {
  return <div className="cart-item">{children}</div>;
}

// the children prop is a special prop that allows you to pass content between the opening and closing tags of a component. It's often used for components that act as containers or wrappers for their content.
